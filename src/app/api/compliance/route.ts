import { NextRequest,NextResponse } from 'next/server';import { requireManager } from '@/lib/auth';import { complianceQuerySchema } from '@/lib/compliance';import { handleApiError } from '@/lib/errors';import { getCompliance } from '@/lib/services/complianceService';export async function GET(request:NextRequest){try{const input=complianceQuerySchema.parse(Object.fromEntries(request.nextUrl.searchParams));return NextResponse.json({success:true,...await getCompliance(await requireManager(),input)},{headers:{'Cache-Control':'private, no-store'}})}catch(error){return handleApiError(error)}}
