import { NextRequest, NextResponse } from 'next/server'; import { requireAdmin } from '@/lib/auth'; import { handleApiError } from '@/lib/errors';
import { demoPasswordsSchema } from '@/lib/adminSchemas'; import { assertAdminMutationRequest, noStoreHeaders } from '@/lib/adminSecurity'; import { generateDemoPasswords } from '@/lib/services/adminService';
export async function POST(request: NextRequest) { try { const admin = await requireAdmin(); assertAdminMutationRequest(request, admin.id, 'demo-passwords'); const body = demoPasswordsSchema.parse(await request.json()); return NextResponse.json({ success: true, credentials: await generateDemoPasswords(admin, body.userIds, body.forceChange) }, { headers: noStoreHeaders() }); } catch (error) { return handleApiError(error); } }
