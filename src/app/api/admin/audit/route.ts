import { NextRequest, NextResponse } from 'next/server'; import { requireAdmin } from '@/lib/auth'; import { handleApiError } from '@/lib/errors'; import { noStoreHeaders } from '@/lib/adminSecurity'; import { listAdminAudit } from '@/lib/services/adminService';
export async function GET(request: NextRequest) { try { const params = request.nextUrl.searchParams; return NextResponse.json({ success: true, events: await listAdminAudit(await requireAdmin(), { action: params.get('action') ?? undefined, targetId: params.get('targetId') ?? undefined }) }, { headers: noStoreHeaders() }); } catch (error) { return handleApiError(error); } }
