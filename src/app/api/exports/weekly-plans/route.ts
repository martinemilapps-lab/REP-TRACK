import { NextRequest } from 'next/server';import { requireAuthenticatedUser } from '@/lib/auth';import { handleApiError } from '@/lib/errors';import { weeklyPlanExportSchema } from '@/lib/exportSchemas';import { buildWeeklyPlansExport } from '@/lib/services/exportService';import { workbookResponse } from '@/lib/exportWorkbook';
export async function GET(request:NextRequest){try{const session=await requireAuthenticatedUser();const input=weeklyPlanExportSchema.parse(Object.fromEntries(request.nextUrl.searchParams));return workbookResponse(await buildWeeklyPlansExport(session,input),`REP_TRACK_${input.team==='true'?'Team_':'My_'}Weekly_Plans_${input.weekStart??new Date().toISOString().slice(0,10)}`)}catch(error){return handleApiError(error)}}
