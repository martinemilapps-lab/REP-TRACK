import { NextRequest,NextResponse } from 'next/server';import { requireAuthenticatedUser } from '@/lib/auth';import { handleApiError } from '@/lib/errors';import { salesAnalyticsQuerySchema } from '@/lib/salesAnalytics';import { getSalesAnalytics } from '@/lib/services/salesAnalyticsService';
export async function GET(request:NextRequest){try{const q=Object.fromEntries(request.nextUrl.searchParams);const input=salesAnalyticsQuerySchema.parse(q);return NextResponse.json({success:true,...await getSalesAnalytics(await requireAuthenticatedUser(),input)},{headers:{'Cache-Control':'private, no-store'}})}catch(error){return handleApiError(error)}}
