import {NextResponse}from'next/server';import{desc,eq}from'drizzle-orm';import{requireAuthenticatedUser}from'@/lib/auth';import{db,representativeVisitRates}from'@/lib/db';import{handleApiError}from'@/lib/errors';import{resolveWritableRepId}from'@/lib/repAccessPolicy';
export async function GET(){try{const session=await requireAuthenticatedUser();const repId=resolveWritableRepId(session);const all=await db.select().from(representativeVisitRates).where(eq(representativeVisitRates.repId,repId)).orderBy(desc(representativeVisitRates.effectiveFrom)).all();const latest=[...new Map(all.map(rate=>[rate.customerCategory,rate])).values()].map(rate=>({...rate,requiredPerWeek:rate.dailyRate*rate.workingDaysPerWeek,requiredPerMonth:rate.dailyRate*rate.workingDaysPerMonth}));return NextResponse.json({success:true,rates:latest},{headers:{'Cache-Control':'private, no-store'}})}catch(e){return handleApiError(e)}}
