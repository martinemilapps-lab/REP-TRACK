'use client';import{useState}from'react';import{Button}from'@/components/ui/Button';import{FormField}from'@/components/ui/FormField';import{useTranslation}from'@/lib/i18nContext';export function SpecialTaskForm({selectedRep,onSuccess,onError}:{selectedRep:string;onSuccess:(x:string)=>void;onError:(x:string)=>void}){void selectedRep;const{language}=useTranslation(),ar=language==='ar',l=(e:string,a:string)=>ar?a:e;const[title,setTitle]=useState(''),[description,setDescription]=useState(''),[saving,setSaving]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setSaving(true);try{const r=await fetch('/api/reports/special-tasks',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title,description})});const x=await r.json();if(!r.ok)throw new Error(x.message);onSuccess(l('Sales Review / Admin Work saved.','تم حفظ مراجعة المبيعات / العمل الإداري.'));setTitle('');setDescription('')}catch(reason){onError(reason instanceof Error?reason.message:'Unable to save')}finally{setSaving(false)}}return <form onSubmit={submit} className="space-y-4"><h2 className="text-2xl font-black">{l('Sales Review / Admin Work','مراجعة المبيعات / العمل الإداري')}</h2><FormField required label={l('Sales Review / Admin Work','مراجعة المبيعات / العمل الإداري')} value={title} onChange={setTitle}/><FormField required multiline label={l('Description','الوصف')} value={description} onChange={setDescription}/><Button type="submit" isLoading={saving}>{l('Submit','إرسال')}</Button></form>}
