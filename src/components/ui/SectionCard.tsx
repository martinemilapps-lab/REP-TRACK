import type { HTMLAttributes, ReactNode } from 'react';
export function SectionCard({ title, description, actions, className='', children, ...props }: HTMLAttributes<HTMLElement> & { title?: string; description?: string; actions?: ReactNode }) { return <section className={`section-card ${className}`} {...props}>{(title || actions) && <header className="mb-4 flex items-start justify-between gap-4"><div>{title && <h2 className="font-semibold text-[var(--ink)]">{title}</h2>}{description && <p className="mt-1 text-sm text-[var(--ink-soft)]">{description}</p>}</div>{actions}</header>}{children}</section>; }
