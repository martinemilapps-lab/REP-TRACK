import type { ReactNode } from 'react';
export function DataTable({ headers, children, label }: { headers:string[]; children:ReactNode; label:string }) { return <div className="overflow-x-auto rounded-xl border border-[var(--line)]"><table className="w-full min-w-[680px] border-collapse text-sm" aria-label={label}><thead className="bg-[var(--surface-subtle)]"><tr>{headers.map((header)=><th key={header} scope="col" className="px-4 py-3 text-start text-xs font-semibold text-[var(--ink-soft)]">{header}</th>)}</tr></thead><tbody className="divide-y divide-[var(--line)]">{children}</tbody></table></div>; }
