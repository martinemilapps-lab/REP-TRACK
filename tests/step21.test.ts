import assert from 'node:assert/strict';import{readFileSync}from'node:fs';
const service=readFileSync('src/lib/services/salesAnalyticsService.ts','utf8');assert.match(service,/assertAuthenticatedSession\(session\)/);assert.match(service,/organizationService\.getScopedSalesAssignments\(session\.id\)/);assert.match(service,/outside your authorized hierarchy scope/);assert.doesNotMatch(service,/positionCode\s*===\s*['"]SMD/);assert.match(service,/PERSONAL_MR|ScopedSalesAssignments/);assert.doesNotMatch(service,/insert\(|update\(|delete\(/,'analytics is read-only');const route=readFileSync('src/app/api/sales/analytics/route.ts','utf8');assert.match(route,/requireAuthenticatedUser\(\)/);const availability=readFileSync('src/lib/services/availabilityService.ts','utf8');assert.match(availability,/isAvailable/);const manager=readFileSync('src/components/workspace/ManagerWorkspace.tsx','utf8');const mr=readFileSync('src/components/workspace/MedicalRepWorkspace.tsx','utf8');assert.doesNotMatch(manager,/SalesAnalyticsView/);assert.match(manager,/Product Availability/);assert.match(mr,/SalesAnalyticsView/);assert.match(mr,/AvailabilityForm/);console.log('STEP 21 manager analytics removal and hierarchy-scoped availability tests passed');
